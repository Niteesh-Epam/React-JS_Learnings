import styled from "styled-components";

export const Button = styled.button`
  &.isActive {
    color: blueviolet;
    background: none;
    border: 1px solid #000;
    border-radius: 4px;
    cursor: pointer;
    padding: 6px 10px;
  }
`;
